<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!-- API 11 theme customizations can go here. -->
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyAppBaseTheme" parent="AppBaseTheme">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="popup_select_way">
        <item name="android:windowEnterAnimation">@anim/popup_enter_up</item>
        <item name="android:windowExitAnimation">@anim/popup_out_down</item>
    </style>
    <style name="Mydialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/bottom_background_color</item>
        <item name="android:background">@drawable/my_progress_dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="AppTheme.Splash" parent="android:Theme.Light.NoTitleBar">
        <item name="android:background">@mipmap/guide</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="popup_photo_view_style">
        <item name="android:windowEnterAnimation">@anim/anim_photo_view_in</item>
        <item name="android:windowExitAnimation">@anim/anim_photo_view_out</item>
    </style>
</resources>
